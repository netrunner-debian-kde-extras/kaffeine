Index: control
===================================================================
--- control	(revision 59)
+++ control	(working copy)
@@ -48,3 +48,34 @@
  .
  Authors: Jürgen Kofler and Christophe Thommeret
  Homepage: http://kaffeine.sourceforge.net
+
+Package: kaffeine-dbg
+Section: devel
+Architecture: any
+Priority: extra
+Depends: kaffeine (= ${Source-Version})
+Description: debugging symbols for kaffeine
+ This package contains the debugging symbols associated with kaffeine.
+ They will automatically be used by gdb for debugging kaffeine-related
+ issues.
+
+Package: kaffeine-xine-dbg
+Section: libdevel
+Architecture: any
+Priority: extra
+Depends: kaffeine-xine (= ${Source-Version})
+Description: debugging symbols for kaffeine
+ This package contains the debugging symbols associated with kaffeine-xine.
+ They will automatically be used by gdb for debugging kaffeine-xine-related
+ issues.
+
+Package: kaffeine-gstreamer-dbg
+Section: libdevel
+Architecture: any
+Priority: extra
+Depends: kaffeine-gstreamer (= ${Source-Version})
+Description: debugging symbols for kaffeine
+ This package contains the debugging symbols associated with kaffeine-gstreamer.
+ They will automatically be used by gdb for debugging kaffeine-gstreamer-related
+ issues.
+
Index: rules
===================================================================
--- rules	(revision 59)
+++ rules	(working copy)
@@ -9,6 +9,9 @@
 DEB_CONFIGURE_EXTRA_FLAGS += --disable-rpath
 DEB_CONFIGURE_EXTRA_FLAGS += --with-xorg --with-xinit-workaround
 
+DEB_CONFIGURE_EXTRA_FLAGS += --enable-debug=full
+DEB_DH_STRIP_ARGS  := --dbg-package=kaffeine --dbg-package=kaffeine-xine --dbg-package=kaffeine-gstreamer
+
 DEB_MAKE_INSTALL_TARGET := install DESTDIR=$(CURDIR)/debian/tmp/
 DEB_COMPRESS_EXCLUDE := .docbook
 
